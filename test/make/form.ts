// import { dirname } from 'path'
// import { fileURLToPath } from 'url'
// import fs from 'fs'
// import handle from '~/code/make/table'
// import base from '~/code/type/base'

// const __dirname = dirname(fileURLToPath(import.meta.url))

// start()

// async function start() {
//   const { type, parser } = await handle({ base })

//   fs.mkdirSync(`${__dirname}/../tmp/table`, { recursive: true })
//   fs.writeFileSync(`${__dirname}/../tmp/table/index.ts`, type)
//   fs.writeFileSync(`${__dirname}/../tmp/table/parser.ts`, parser)
// }
