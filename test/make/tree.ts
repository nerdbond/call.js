// import { dirname } from 'path'
// import { fileURLToPath } from 'url'
// import fs from 'fs'
// import handle from '~/code/make/tree'
// import base from '~/code/type/base'

// const __dirname = dirname(fileURLToPath(import.meta.url))

// start()

// async function start() {
//   const { type, parser } = await handle({ base })

//   fs.mkdirSync(`${__dirname}/../code/type/tree`, { recursive: true })
//   fs.writeFileSync(`${__dirname}/../code/type/tree/index.ts`, type)
//   fs.writeFileSync(`${__dirname}/../code/type/tree/parser.ts`, parser)
// }
